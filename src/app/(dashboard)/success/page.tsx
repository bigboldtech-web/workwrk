// WorkwrK Success — coming-soon stub. The route exists so the Apps
// Panel and Product Store don't 404; the real product implementation
// lands when the team pulls it off the roadmap.

import { CatalogStubPage } from "@/components/products/catalog-stub-page";

export default function SuccessPage() {
  return <CatalogStubPage slug="workwrk-success" />;
}
