---
title: Module status
description: What is finished, what is half built, and what is only a shell, so nobody discovers an empty box by clicking it.
---

This page exists so you never open a WorkwrK surface expecting it to work and find out the hard way.

Three labels are used, and they mean exactly this:

- **Built**: you can use it end to end today. Verified in the code.
- **In progress**: the surface exists and part of it works. The specific gap is named. Do not rely on the missing half.
- **Coming soon**: the surface exists but there is no working backend behind it, or the backend exists with no way to reach it. Treat it as a placeholder.

Last verified against the codebase on 2026-08-14. Ordered by area.

## Work chassis

| Module | Where | Status | What you need to know |
| --- | --- | --- | --- |
| Spaces, Folders, Lists | rail Home, `/spaces` | Built | Full create, rename, move, delete through `/api/spaces`, `/api/folders`, `/api/boards`. |
| Tasks (Items) | any List | Built | Create, edit, assign, subtasks, checklists, comments, priority, tags, custom fields, time tracking. Written to the `Item` table. |
| View types | board view tabs | Built | 17 renderers exist: List/Table, Board/Kanban, Calendar, Gantt, Timeline, Workload, Cards, Hierarchy, Pivot, Chart, Dashboard, Form, Doc, Whiteboard, File gallery, Map, Activity. |
| Board title row controls | any board header | Built | Filter and Reader mode were removed (no backend, so they are gone rather than faked). Automate links to `/automation/workflows`, Share opens the share dialog, Ask is a deliberately disabled "Soon" button. |
| Global search and command palette | search box, `Cmd/Ctrl + K` | Built | Rewritten over the real work graph. Indexes Items (tasks), Boards, Spaces, Folders, Docs, Whiteboards and people, plus SOPs, Goals, Meetings, Departments, Ideas, Policies and Announcements, all visibility-gated. The dead finance result types are gone. The empty-state palette shows real recents and wired quick actions; every Command and Navigate row is a real link. |
| Planner | `/planner` | Built | The week grid renders your local tasks and work items for everyone, no connection required. Google Calendar is a dismissible connect banner, not a gate. Two-way Google sync still depends on connecting the integration and the calendar-sync cron. |
| Docs | `/docs`, `/library` | Built | Create, edit, save and delete via `/api/docs`. |
| Whiteboards | `/whiteboards`, `/library` | Built | Create, edit, save and delete via `/api/whiteboards`. |
| Tables | `/tables`, `/library` | Built | Spreadsheet-shaped data grids, with formula columns and the relational link, lookup and rollup trio, saved named views, CSV import and export. The header **Public** toggle really does publish: it opens an unauthenticated read-only page at `/embed/tables/<id>`. |
| Files | `/files`, `/library` | Built | Upload, folder, and link based external attachments. |
| Dashboards | `/dashboards` | Built | Create and configure dashboards with widget cards. |
| Forms | `/forms` | Built | Builder, AI generation from a prompt, a respond page, an embeddable version at `/embed/forms/<id>`, and submissions. |
| Timesheets | `/timesheets` | Built | Weekly sheets, entries, submit and approve. Card timers on board items log automatically. |
| Team workload | `/team/workload` | Built | Manager only, and only if you have reports. Reads live Items. |
| Inbox | `/inbox` | Built | Reads real notification rows, mark read, preferences. See the caveat under Notifications below. |
| Trash | `/trash` | Built | Org wide recycle bin, 60 day window. The rail only shows it to the HR admin tier, but the page has no server gate and `/api/trash` is manager tier, so any manager who types the URL can use it. Archived tasks do not land here. |

## Alignment layer

| Module | Where | Status | What you need to know |
| --- | --- | --- | --- |
| Roles (job titles) | `/people/roles` | Built | Role definition pages, KRA templates per role. |
| KRAs and KPIs | `/kra-kpi` | Built | Create, edit, assign, weight. Weights are validated to never exceed 100 per person, but are not required to reach 100. |
| KRA inheritance | automatic | Built | Hiring into a role seeds its KRAs and published SOPs. Adding a KRA to a role pushes it to existing holders. |
| KPI recording | your profile | Built | Record numbers, direction aware scoring, no invented baselines. |
| KPI approval loop | `/team/kpi-reviews` | Built | Submit, approve, request changes, notification back to the person. |
| Goals and OKRs | `/okrs` | Built | One accountable owner plus contributors resolved at read time. One derived progress number shown everywhere. |
| Key result to KPI linking | goal detail | Built | A linked key result reads the gauge's latest value. Your typed number is preserved, not overwritten. |
| KRA weight in scoring | anywhere a score appears | **Coming soon** | Weights are displayed but no scoring math uses them. A 5% KRA counts the same as a 50% KRA today. |
| Team alignment board | `/team/alignment` | Built | Per report: active KRAs with weights, current period KPI compliance, SOP read rate, weekly review state. Unmeasured people show a dash, never a fake 100%. |
| Director rollup | `/team/rollup` | Built | Director tier and above. Groups by sub manager. |
| Performance score | behind the scenes | Built | Composite monthly score. See the caveat below about the weight editor. |

## Performance and people

| Module | Where | Status | What you need to know |
| --- | --- | --- | --- |
| Review cycles | `/reviews` | Built | Create, launch, self assessment, manager review, calibration, finalize. |
| Appraisal letter | none | Coming soon | `/api/reviews/[id]/appraisal-letter` works but nothing in the interface calls it. |
| Talent 9-box | `/talent` | In progress | You can place people by hand, and auto place from performance scores. Review calibration does **not** feed it. The two systems are disconnected. |
| Weekly reviews | `/team/reviews` | Built | Manager queue, submit and approve. |
| People directory | `/people` | Built | Manager tier for the full directory, scoped to your report tree below the org wide door. |
| Person profile | `/people/[id]`, `/people/me` | Built | Three render modes: self, manage, peer. |
| Departments (Functions) | `/people/departments` | Built | Full CRUD from the interface: create, rename, edit description and colour, assign a department head, re-parent into a nested tree, and delete (the server refuses to delete a department that still has members). Tree and Grid views with rolled-up headcount. No merge/archive yet. |
| Org chart | `/organization` | Built | Reads the manager chain. |
| Invite a person | invite modal | Built | The modal now collects department, role and manager (all optional) alongside the required KRAs and SOPs, so a hire can land placed. Accept-invite seeds each KRA's role-level weight when set and only even-splits the rest, instead of flat even weights across the board. |
| Candor | `/candor` | **Coming soon** | You can create a candor round. Employees cannot respond and nobody can see results: `/candor/[id]` does not exist and the respond and results endpoints have zero callers. |
| Surveys | `/surveys` | **Coming soon** | Same shape. You can create a survey. `/surveys/[id]` does not exist, and the respond and responses endpoints have zero callers. |
| Announcements | `/announcements` | In progress | You can read announcements. The Acknowledge button posts to `/api/announcements/<id>/ack` while the route is `/acknowledge`, so acknowledging always fails. **New announcement** does post a real row, but it is a quick add that writes a fixed placeholder titled "Untitled announcement": there is no composer and no way to edit the text afterwards. |
| Kudos | `/kudos` | In progress | Giving and reacting work and feed the performance score. The leaderboard API has no interface. |
| Assets | `/assets` | **Coming soon** | The list reads real data. "Add asset" is a button with no handler, and create, edit and delete are unreachable. |
| Tools and subscriptions | `/tools` | Built | Reads and writes real data. |

## Knowledge

| Module | Where | Status | What you need to know |
| --- | --- | --- | --- |
| SOPs | `/sops` | Built | Create in one of three types (Written, Checklist, Click-capture), version with rollback, publish, archive, assign, track progress and compliance. Folder based visibility with inheritance. AI generation is not a create option: it is an in-editor action on a checklist SOP that drafts steps from the title and description. |
| SOP folders and tags admin | none | **Coming soon** | The folder manager and the tags manager components are fully written and imported by nothing. You cannot rename or merge a tag today. |
| SOP review and approval flow | `/sops/[id]` | In progress | Five statuses exist (Draft, In review, Approved, Published, Archived) and you can filter by them. The only status transitions the detail page offers are Publish and Archive (the same menu also carries Assign and Run, which are not status changes). There is no assigned reviewer, no approve action and no notification, so "In review" and "Approved" are labels you set, not a workflow. |
| Process runs | `/process-runs` | Built | Run history, plus a public share link at `/run/<token>`. |
| Policies | `/policies` | Built | Create, assign, acknowledge, compliance view, ledger export. |
| Contracts and agreements | `/agreements` | Built | Create, templates, parties, send, signing at `/sign/<token>`. |

## Build and extend

| Module | Where | Status | What you need to know |
| --- | --- | --- | --- |
| Automation | `/automation/workflows` | In progress | The engine, workflow CRUD, runs, retries, logs and health pages are real. Only 5 of the 16 declared triggers ever fire, and 3 of those only from the API and integration paths, not from in-app actions. Rules on "task created", "KPI recorded" and "kudos created" will not fire from the app. |
| Build (app builder) | `/build` | In progress | Describe an app in a prompt, it generates typed fields, and `/build/<slug>` renders a table where you add, edit and delete rows. Real backend. It builds simple data apps, not interfaces or logic. |
| Marketplace | `/store` | **Coming soon** | Entirely hardcoded. The install state is local component state and nothing is installed. |
| AI agents | `/agents` | **Coming soon** | Entirely hardcoded, including the usage metrics shown on each agent card. Nothing is connected. |
| AI Sidekick | `/sidekick` | Built | Real chat against `/api/ai`. |
| Clips | `/notetaker` | In progress | The rail calls it Clips with a video icon and the sidebar says "All Clips". Nothing records. The page is an AI notetaker: paste a meeting transcript and it extracts decisions, action items and attendees. That part works. |
| Integrations | `/integrations` | **Coming soon** | Honest catalog preview: connectors are browsable by category and every one reads "Coming soon". No fabricated "installed" flags and no connect action. (The Planner's own Google hook is a separate surface.) |

## Settings

Settings is now a two-door nav. The **Admin** door (org configuration) is hidden entirely for anyone below Company Admin or Super Admin, and its most sensitive pages (`/settings/identity`, `/settings/tags`, `/settings/structure`, `/settings/audit`, `/settings/data`, `/settings/defaults`) also enforce a server-side redirect so a manager cannot reach them by URL. The **Personal** door (`/account/*` plus Notifications) is available to everyone.

| Page | Status | What you need to know |
| --- | --- | --- |
| `/settings` | Built | Admin-door overview: an org summary card grid that deep-links to the editors. |
| `/settings/identity` | Built | Saves org name, primary domain, logo upload, and the company profile (industry, mission, vision, about, values). The profile feeds AI KRA/KPI generation. This replaced a save that used to 400. |
| `/settings/locale` | Built | Saves to org settings. |
| `/settings/modules` | Built | Saves to org settings, but nothing in the app reads the list; app visibility is by access tier. |
| `/settings/tags` | In progress | Create only. It never calls the tag update or delete endpoints; the delete control only drops the row from the current screen. |
| `/settings/task-types` | In progress | Create, delete and set default all work. There is no edit control: the only field the page patches is the default flag, so a type's name and icon are fixed at creation. |
| `/settings/members` | Built | Members and invitations. Inline access-level and manager edits for admins. |
| `/settings/structure` | Built | New. Org-structure hub: live counts for Functions (Departments), Roles and Offices, plus a read-only Levels explainer with per-rung head counts. Offices viewer is Coming soon; Levels are a fixed enum, explained not edited. |
| `/settings/hierarchy` | In progress | Read only. It renders the reporting structure from the user list. You cannot change a reporting line here. |
| `/settings/permissions` | In progress | The matrix saves, but only 10 of 439 API route files consult it, covering five of the sixteen modules (`kras`, `sops`, `assets`, `policies`, `announcements`). Everything else uses hardcoded Level ladders. |
| `/settings/audit` | Built | Reads the real activity engine at `/api/audit`: persisted rows, cursor-paginated, filterable by type/actor/date. The header Export button downloads the signed log through `/api/audit-log/export`. No sample data. |
| `/settings/data` | Built | New. Admin-only export hub over endpoints that existed with no UI: full-org ZIP (`/api/export/all`), people CSV (`/api/export/people`), and timesheets/purchase-orders/invoices/audit CSVs (`/api/export/[type]`), plus links to import and Trash. |
| `/settings/defaults` | Built | New. Writes the org-wide preference system (`/api/org/preferences`): default appearance/accent/density that seed members, and locks (theme, density, sidebar, Home cards) that re-stamp on every load so members can't override them. |
| `/settings/api` | Built | Thin client over the real `/api/keys` engine: mint (secret shown once), list with scopes/last-used/request-count, and real soft-delete revoke. Admins only. No browser-side fabrication. |
| `/settings/calendar` | **Coming soon** | Honest placeholder. External calendar sync (Google, Outlook, iCloud, Fastmail, ICS) is shown as planned providers; the page makes no network calls and fabricates no connections. |
| `/settings/integrations` | Built | A static navigation hub, which is all it claims to be. |
| `/settings/import-export` | Built | A static navigation hub. Its audit-log export link now leads to a working export. |
| `/settings/billing` | Built | Opens a real Stripe customer portal, and returns a clear "Stripe not configured" error when billing keys are absent. Org admin only. |
| `/settings/scoring` | In progress | Cadences, bands and anchors save correctly. The weight sliders edit KPI, SOP compliance, Behavioral and Peer, but the scoring engine reads KPI, Manager, Peer, Self and SOP compliance. Behavioral is ignored, and Manager and Self always use defaults. |
| `/settings/notifications` | Built | The single merged personal notifications page (Inbox + Email). It reconciles both historical stores (`/api/preferences` JSON store and `/api/email-preferences`) behind one surface. Unproduced types render as disabled "Soon" rows. |
| `/account/profile` | Built | |
| `/account/appearance` | Built | Anything an admin locked in Defaults & locks is greyed out here. |
| `/account/notifications` | Built | Retired: redirects to `/settings/notifications`. |
| `/account/security` | Built | Reads real MFA + email-verified status and the org policy. Resend verification, Enable/Turn-off two factor (real TOTP enrol + backup codes via `/api/auth/mfa/enroll`) all work. The old hardcoded "2 active sessions" line is gone. |

## Notifications and reminders

Wave 1 closed most of the old gap here. What is true now, precisely:

| Thing | Status | What you need to know |
| --- | --- | --- |
| Notifications bell | Built | Top right, a bell of its own separate from reminders. Unread-count badge, popover of recent notifications with Mark all read, each row deep-links. Polls `/api/inbox/count` every 25 seconds and on tab focus. |
| Task notifications | Built | The Item pipeline emits through `src/lib/notify-item.ts`: assign and reassign notify the new owner, a status change notifies the owner plus everyone who commented, comment mentions notify the mentioned. All gated by `/settings/notifications` prefs; the actor is never notified about their own action. |
| Due-date notifications | In progress | `notifyItemsDueToday` (the due-today + overdue sweep) is written and idempotent, but nothing calls it on a schedule yet, so it does not fire automatically. Assign and status notifications are unaffected. |
| Desktop notifications | Built | When the unread count climbs while the tab is backgrounded, the bell raises a browser notification + chime for the newest item. Permission is requested only from the popover's "Enable desktop alerts" button, never on load. Muting silences the popup, not the badge. |
| Other notification rows | Built | Many modules also write real `Notification` rows: KPI approvals, KRA assignment, SOP assignment, announcements, policies, kudos, meetings, goals, timesheets. |
| Reminders bell | Built | Top right. Groups Fired, Overdue, Today and Upcoming, plus a read-only "Recently done" history (last 14 days). Snooze (+1h) and dismiss work. Polls every 60 seconds. |
| Reminder firing | Built | A fired reminder is a persistent bottom-right popup (Snooze 10m / 1h / tomorrow, Dismiss, Open), not a 3-second toast. It survives reloads: the fired-but-unacted set is reloaded on mount, so nothing is lost by looking away. |
| Reminder cron in production | **Needs manual registration** | The in-app ticker fires reminders while the app is open. `/api/cron/reminders` exists and is documented in `scripts/CRON-SETUP.md`, but it must be added to the aaPanel cron by hand. Until it is, reminders do not fire for users with the app closed. |

## The legacy task table

Worth stating plainly because it explains several oddities above.

There are two task tables. Everything you create on a List is an `Item`. A separate legacy `Task` table still exists and is written only by `/api/tasks`.

Three places still read the legacy table and therefore cannot see your board work: `/analytics`, the performance score's task completion component, and `/api/tasks/workload`. Global search was a fourth until Wave 1 rewrote it over the `Item` graph. Migrating the rest is a tracked debt.

## Reporting an inaccuracy

If you find something on this page that does not match what you see, that is a bug in this page, and it matters more than most bugs. Say so.
