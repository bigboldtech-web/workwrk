; next-auth@4 peer-wants nodemailer@^7 but the project uses nodemailer@^8.
; The lockfile was resolved with peer-overriding on, so install with
; legacy-peer-deps everywhere (CI runner, prod server, local) to match it
; and keep `npm ci` deterministic.
legacy-peer-deps=true
